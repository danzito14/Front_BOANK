import { Unsolonumero } from './unsolonumero';

describe('Unsolonumero', () => {
  it('should create an instance', () => {
    const directive = new Unsolonumero();
    expect(directive).toBeTruthy();
  });
});
