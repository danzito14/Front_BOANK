import { Solonuermos } from './solonuermos';

describe('Solonuermos', () => {
  it('should create an instance', () => {
    const directive = new Solonuermos();
    expect(directive).toBeTruthy();
  });
});
