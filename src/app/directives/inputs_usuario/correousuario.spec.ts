import { Correousuario } from './correousuario';

describe('Correousuario', () => {
  it('should create an instance', () => {
    const directive = new Correousuario();
    expect(directive).toBeTruthy();
  });
});
